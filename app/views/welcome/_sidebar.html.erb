<script>
  var new_login = <%= @new_login ? "true" : "false" %>;
  window.onload = function(){
    // This serves as a proxy for "Are we on a page requiring login?"
    if (document.getElementById('logout')) {
      if (new_login) {
        // Expand the "New User Account Created" head to draw attention to it
        $('#new_login').animate({fontSize: '200%'}, 600);
        // Likewise the "Logout" button, so they see the name
        $('#logout').animate({fontSize: '150%'}, 600);
        // Shrink the "Logout" button back to normal, and shrink the CNA head
        $('#logout').animate({fontSize: '100%'}, 800,
          function() {
            $('#new_login').animate({fontSize:'90%'},100);
          }
        );
      }
    }
  };
</script>

<h2><%= link_to "WISP:<br>Wisconsin Irrigation Scheduling Program".html_safe, root_path %></h2>
<br>
<br>
<ul style="font-size:small;">
  <li><%= link_to "WISP Home", root_path %></li>
  <li>&nbsp;</li>
  <li><%= link_to "Farm Status", "/wisp/farm_status" %></li>
  <li><%= link_to "Pivots, Fields, and Crops", "/wisp/pivot_crop" %></li>
  <li><%= link_to "Field Status", '/wisp/field_status' %></li>
  <li><%= link_to "Field Groups", '/weather_stations' %></li>
  <li><%= link_to "Multi-Edit Daily Data", '/wisp/weather' %></li>
  <li>&nbsp;</li>
  <li><%= link_to "Project Status", '/wisp/project_status' %></li>
  <li><%= link_to "Download User's Guide", guide_welcome_index_path %></li>
  <li><%= mail_to "agweather@cals.wisc.edu", "Contact us" %></li>

  <%# Report setup not implemented so this hides the link %>
  <% if false %><li><%= link_to "Report Setup", '/wisp/report_setup' %> </li><% end %>

  <% if current_user && current_user.admin? %>
    <li><%= link_to "Admin Users", users_path %></li>
  <% end %>
</ul>
<br>
<br>
<br>

<% if user_signed_in? %>
  Signed in as <%= current_user.email %>.
  <br>
  <br>
  <%= button_to "Manage Account", edit_user_registration_path, method: :get %>
  <br>
  <br>
  <div style="float: left; width: auto; margin-top: 0.3em; margin-right:0.5em;">Not you?</div>
  <%= button_to "Sign out", destroy_user_session_path, method: :delete %>
<% else %>
  <%= link_to "Sign up", new_user_registration_path %>
  <%= link_to "Sign in", new_user_session_path %>
<% end %>

<% if flash[:notice] %>
  <br>
  <%= flash[:notice] %>
<% end %>
<% if flash[:error] %>
  <br>
  <%= flash[:error] %>
<% end %>
<div id='save-data-notice'>
  <p><center>REMINDER</center></p>
  <p>WISP user input data will be purged on Feb. 15<sup>th</sup> each year. To save current year data use the <u>Create Report in CSV Format</u> on the Field Status page prior to 2/15!</p>
</div>
